import 'package:cash_book/controllers/current_user_controller.dart';
import 'package:get/get.dart';

void addNewEntry() {
  var user = Get.find<CurrentUserController>().currentUser.value;
  // Entry newEntry = Entry(entryId: .userBooks, bookId: bookId, entryAmount: entryAmount, entryDate: entryDate, entryTime: entryTime, entryLastModified: entryLastModified, entryType: entryType, entryUserId: entryUserId)
}
